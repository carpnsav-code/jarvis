"""Turn-taking logic: when to take the turn (endpointing), how to chunk the
reply into sentences, and when NOT to reply at all (sign-off detection).

Everything in this package is provider-independent and has no SDK imports, so
it is fully unit-testable without any API keys.
"""
